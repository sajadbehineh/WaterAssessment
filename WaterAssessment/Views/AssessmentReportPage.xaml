<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WaterAssessment.Views.AssessmentReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WaterAssessment.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:localControls="using:WaterAssessment.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WaterAssessment.Models"
    xmlns:primitives="using:CommunityToolkit.WinUI.UI.Controls.Primitives"
    xmlns:toolkitGrid="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:RowIndexConverter
            x:Key="RowIndexConverter"
            CurrentPage="{x:Bind ViewModel.CurrentPage, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.Assessments, Mode=OneWay}"
            PageSize="10" />

        <converters:DateTimeToShamsiConverter x:Key="ShamsiConv" />
        <converters:DateTimeToDateTimeOffsetConverter x:Key="DateToOffsetConv" />
        <converters:AssessmentEmployeesToDisplayConverter x:Key="AssessmentEmployeesToDisplayConverter" />

        <Style x:Key="CenterCellTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,20"
            Spacing="10">
            <TextBlock
                FontFamily="{StaticResource SamimBold}"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="گزارشات اندازه گیری ‌ها" />

            <StackPanel
                Padding="10"
                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                CornerRadius="8"
                Orientation="Horizontal"
                Spacing="15">

                <StackPanel>
                    <TextBlock
                        FontFamily="{StaticResource SamimBold}"
                        FontWeight="Normal"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="مکان:" />
                    <ComboBox
                        Width="200"
                        DisplayMemberPath="LocationName"
                        ItemsSource="{x:Bind ViewModel.Locations}"
                        PlaceholderText="همه مکان‌ها"
                        SelectedItem="{x:Bind ViewModel.FilterLocation, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel>
                    <TextBlock
                        FontFamily="{StaticResource SamimBold}"
                        FontWeight="Normal"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="نوع مکان:" />
                    <ComboBox
                        Width="200"
                        DisplayMemberPath="Title"
                        ItemsSource="{x:Bind ViewModel.LocationTypes}"
                        PlaceholderText="همه نوع مکان"
                        SelectedItem="{x:Bind ViewModel.FilterLocationType, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel>
                    <TextBlock
                        FontFamily="{StaticResource SamimBold}"
                        FontWeight="Normal"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="اندازه‌گیر:" />
                    <ComboBox
                        Width="220"
                        DisplayMemberPath="LastName"
                        ItemsSource="{x:Bind ViewModel.Employees}"
                        PlaceholderText="همه اندازه‌گیرها"
                        SelectedItem="{x:Bind ViewModel.FilterEmployee, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel>
                    <TextBlock
                        FontFamily="{StaticResource SamimBold}"
                        FontWeight="Normal"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="از تاریخ:" />
                    <CalendarDatePicker
                        Width="150"
                        CalendarIdentifier="PersianCalendar"
                        Date="{x:Bind ViewModel.FilterStartDate, Mode=TwoWay}"
                        DateFormat="{}{year.full}/{month.integer}/{day.integer}"
                        FontFamily="{StaticResource SamimRegular}"
                        FontWeight="Normal"
                        Foreground="Gray"
                        Language="fa-IR"
                        PlaceholderText="انتخاب تاریخ ..." />
                </StackPanel>

                <StackPanel>
                    <TextBlock
                        FontFamily="{StaticResource SamimBold}"
                        FontWeight="Normal"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="تا تاریخ:" />
                    <CalendarDatePicker
                        Width="150"
                        CalendarIdentifier="PersianCalendar"
                        Date="{x:Bind ViewModel.FilterEndDate, Mode=TwoWay}"
                        DateFormat="{}{year.full}/{month.integer}/{day.integer}"
                        FontFamily="{StaticResource SamimRegular}"
                        FontWeight="Normal"
                        Foreground="Gray"
                        Language="fa-IR"
                        PlaceholderText="انتخاب تاریخ ..." />
                </StackPanel>

                <Button
                    VerticalAlignment="Bottom"
                    Command="{x:Bind ViewModel.ApplyFiltersCommand}"
                    Content="اعمال فیلتر"
                    FontFamily="{StaticResource SamimMedium}"
                    Style="{ThemeResource AccentButtonStyle}" />

                <Button
                    VerticalAlignment="Bottom"
                    Command="{x:Bind ViewModel.ClearFiltersCommand}"
                    Content="حذف فیلترها"
                    FontFamily="{StaticResource SamimMedium}" />
            </StackPanel>
        </StackPanel>

        <toolkitGrid:DataGrid
            x:Name="ReportDataGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            BorderBrush="{ThemeResource SystemControlBackgroundBaseMediumLowBrush}"
            BorderThickness="1"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            DataContext="{x:Bind ViewModel}"
            GridLinesVisibility="Horizontal"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{x:Bind ViewModel.Assessments}"
            Tag="{x:Bind ViewModel}">

            <toolkitGrid:DataGrid.ColumnHeaderStyle>
                <Style TargetType="primitives:DataGridColumnHeader">
                    <Setter Property="Height" Value="42" />
                    <Setter Property="FontFamily" Value="{StaticResource SamimMedium}" />
                    <Setter Property="FontWeight" Value="Normal" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="primitives:DataGridColumnHeader">
                                <Grid Background="{ThemeResource SystemAccentColor}">
                                    <ContentPresenter
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding Content}"
                                        Foreground="White" />
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </toolkitGrid:DataGrid.ColumnHeaderStyle>

            <toolkitGrid:DataGrid.CellStyle>
                <Style TargetType="toolkitGrid:DataGridCell">
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="FontFamily" Value="{StaticResource SamimFD}" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </toolkitGrid:DataGrid.CellStyle>

            <toolkitGrid:DataGrid.RowStyle>
                <Style TargetType="toolkitGrid:DataGridRow">
                    <Setter Property="MinHeight" Value="44" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                </Style>
            </toolkitGrid:DataGrid.RowStyle>

            <toolkitGrid:DataGrid.Columns>

                <toolkitGrid:DataGridTemplateColumn
                    Width="SizeToCells"
                    MinWidth="52"
                    Header="ردیف">
                    <toolkitGrid:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{Binding Converter={StaticResource RowIndexConverter}}" />
                        </DataTemplate>
                    </toolkitGrid:DataGridTemplateColumn.CellTemplate>
                </toolkitGrid:DataGridTemplateColumn>

                <toolkitGrid:DataGridTextColumn
                    Width="200"
                    Binding="{Binding Location.LocationName}"
                    ElementStyle="{StaticResource CenterCellTextStyle}"
                    Header="نام مکان" />

                <toolkitGrid:DataGridTextColumn
                    Width="220"
                    Binding="{Binding AssessmentEmployees, Converter={StaticResource AssessmentEmployeesToDisplayConverter}}"
                    ElementStyle="{StaticResource CenterCellTextStyle}"
                    Header="اندازه‌گیرها" />

                <toolkitGrid:DataGridTextColumn
                    Width="120"
                    Binding="{Binding Date, Converter={StaticResource ShamsiConv}}"
                    ElementStyle="{StaticResource CenterCellTextStyle}"
                    Header="تاریخ" />

                <toolkitGrid:DataGridTextColumn
                    Width="120"
                    Binding="{Binding TotalFlow, Converter={StaticResource DoubleToStringConv}, ConverterParameter='N3'}"
                    ElementStyle="{StaticResource CenterCellTextStyle}"
                    Header="دبی کل (m3/s)" />

                <toolkitGrid:DataGridTemplateColumn Width="100" Header="عملیات">
                    <toolkitGrid:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="models:Assessment">
                            <StackPanel
                                HorizontalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="10">

                                <Button
                                    Padding="4"
                                    Click="OnEditClick"
                                    DataContext="{Binding}"
                                    ToolTipService.ToolTip="ویرایش">
                                    <FontIcon FontSize="14" Glyph="&#xE70F;" />
                                </Button>

                                <Button
                                    Padding="4"
                                    Background="Red"
                                    Click="OnDeleteClick"
                                    DataContext="{Binding}"
                                    Foreground="White"
                                    ToolTipService.ToolTip="حذف">
                                    <FontIcon FontSize="14" Glyph="&#xE74D;" />
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </toolkitGrid:DataGridTemplateColumn.CellTemplate>
                </toolkitGrid:DataGridTemplateColumn>

            </toolkitGrid:DataGrid.Columns>
        </toolkitGrid:DataGrid>

        <localControls:PaginationControl
            Grid.Row="2"
            HorizontalAlignment="Center"
            CurrentPage="{x:Bind ViewModel.CurrentPage, Mode=TwoWay}"
            FlowDirection="LeftToRight"
            PageChangedCommand="{x:Bind ViewModel.PageChangedCommand}"
            PageSize="10"
            TotalItemCount="{x:Bind ViewModel.TotalAssessments, Mode=OneWay}" />
    </Grid>
</Page>
