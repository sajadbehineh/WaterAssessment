<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WaterAssessment.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WaterAssessment.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:WaterAssessment.Models.ViewModel"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel
            Width="300"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="15">

            <TextBlock
                HorizontalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="ورود به سیستم" />

            <TextBox Header="نام کاربری" Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <PasswordBox x:Name="TxtPassword" Header="رمز عبور" />

            <Button
                HorizontalAlignment="Stretch"
                Command="{x:Bind ViewModel.LoginCommand}"
                CommandParameter="{x:Bind TxtPassword}"
                Content="ورود"
                Style="{ThemeResource AccentButtonStyle}" />

            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

            <InfoBar
                IsClosable="True"
                IsOpen="{x:Bind ViewModel.IsErrorVisible, Mode=OneWay}"
                Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                Severity="Error" />
        </StackPanel>
    </Grid>
</Page>
