<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WaterAssessment.Views.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:viewModel="using:WaterAssessment.Models.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewModel:UserManagementViewModel />
    </Page.DataContext>

    <Grid FlowDirection="RightToLeft" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- فرم افزودن کاربر جدید -->
        <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,20" MaxWidth="400">
            <TextBlock Text="افزودن کاربر جدید" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBox Header="نام کاربری" Text="{Binding NewUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox x:Name="NewUserPasswordBox" Header="رمز عبور"/>
            <TextBox Header="نام کامل" Text="{Binding NewFullName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Header="نقش" SelectedItem="{Binding SelectedRole, Mode=TwoWay}">
                <x:String>Admin</x:String>
                <x:String>User</x:String>
            </ComboBox>
            <Button Content="افزودن کاربر" Command="{Binding AddUserCommand}" CommandParameter="{x:Bind NewUserPasswordBox}" HorizontalAlignment="Stretch"/>
        </StackPanel>

        <!-- لیست کاربران -->
        <controls:DataGrid
            Grid.Row="1"
            ItemsSource="{Binding Users}"
            AutoGenerateColumns="False"
            CanUserSortColumns="True"
            IsReadOnly="True">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="نام کاربری" Binding="{Binding Username}" Width="*"/>
                <controls:DataGridTextColumn Header="نام کامل" Binding="{Binding FullName}" Width="*"/>
                <controls:DataGridTextColumn Header="نقش" Binding="{Binding Role}" Width="SizeToCells"/>
                <controls:DataGridTemplateColumn Header="عملیات">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="حذف" 
                                    Command="{Binding DataContext.DeleteUserCommand, ElementName=UserManagementGrid}"
                                    CommandParameter="{Binding}"
                                    Style="{ThemeResource AccentButtonStyle}"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <!-- یک نام به گرید اصلی بدهید تا بتوانید به ViewModel آن از داخل DataTemplate دسترسی پیدا کنید -->
        <Grid x:Name="UserManagementGrid" />
    </Grid>
</Page>
